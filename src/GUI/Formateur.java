package GUI;
import java.util.Date;
public class Formateur extends User {
    public Formateur(String nom, String prenom, Date dateNaissance, String adresse, String login, String motDePass) {
        super(nom, prenom, dateNaissance, adresse, login, motDePass);
    }

}
